Here is exactly what I will do, step-by-step:

1. Parse your inputs: I will separate the Manufacturer Name, Product Number (MPN/SKU), and Product Description.  
2. Locate the official domain: I will search for the manufacturer’s official website. I will *ignore* any third-party resellers (like Amazon, eBay, DigiKey, Grainger, or local distributors) and only look for URLs that belong strictly to the manufacturer's own domain (e.g., `www.[manufacturer].com` or `.[manufacturer].io`).  
3. Construct a targeted search: I will run a web search using a query specifically designed to find the exact product page. It will look something like this:  
   * `site:[manufacturer's official domain] "[exact product number]" [product description]`  
   * *Why?* The `site:` command forces the search engine to only look on their official website, and putting quotes around the product number forces an exact match.  
4. Verify the match: Once I get search results, I will click through or scan the snippets to confirm that the page title and URL clearly contain your specific product number and match the description you gave me.  
5. Deliver the URL: I will provide you with the direct, exact product page URL from that official manufacturer site.

---

What happens if I CANNOT find it:

I will not give you a random or guessed URL. Instead, I will tell you exactly why I failed and ask for clarification, for example:

* *"I found the manufacturer's site, but the product number you gave doesn't return any results. Could it be an older/discontinued model?"*  
* *"I found a product with that number, but the description doesn't match at all—did you mean \[correct description\]?"*  
* *"The official site requires a login portal for that specific part. I can give you the main product series page, but I cannot access the exact dynamic URL without logging in."*  
* *"I only found this part on third-party distributor sites, not on the manufacturer's own website. Would you like the distributor link instead?"*

Important caveat: If your product number is very generic (e.g., "Model 100") or the manufacturer's site has terrible search engine optimization, I may ask you for an additional detail (like a category name or series name) to narrow it down.

Go ahead and give me the information whenever you're ready\!  
