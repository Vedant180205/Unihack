'use client'
import { useMemo, useState } from 'react'
import { ArrowRight, Check, CheckCircle2, ChevronDown, Search, SlidersHorizontal, Sparkles, X } from 'lucide-react'
import { toast } from 'sonner'
import { AppShell, SectionHeading, usePendingQueue } from '@/components/app-shell'

type Item = { sku: string; name: string; category: string; confidence: number; invoice: string; mobile: string; status: string }
const seed: Item[] = [
 { sku:'4816AF', name:'Hex Bolt M8 x 40mm Zinc', category:'Fasteners', confidence:88, invoice:'HEX BOLT M8X40 ZP', mobile:'Hexagonal head machine bolt, zinc plated steel, M8 thread x 40mm length', status:'Review' },
 { sku:'77BC21', name:'Pressure Gauge 0–10 bar', category:'Instrumentation', confidence:97, invoice:'PRESS GAUGE 0-10 BAR', mobile:'Industrial pressure gauge with 0 to 10 bar range and bottom connection', status:'Approved' },
 { sku:'2DE901', name:'Cable Gland M20 IP68', category:'Electrical', confidence:84, invoice:'CABLE GLAND M20 IP68', mobile:'Nylon cable gland with M20 thread and IP68 ingress protection rating', status:'Review' },
 { sku:'A1C490', name:'Nitrile Safety Gloves L', category:'Safety', confidence:92, invoice:'NITRILE GLOVES LARGE', mobile:'Reusable nitrile coated work gloves, large size, cut resistance level B', status:'Approved' },
]
function Counter({ label, value, type }: { label:string; value:string; type:'invoice'|'mobile' }) { const count=value.length; const valid=type==='invoice'?count<=40:count>=60&&count<=80; return <div className={`flex items-center justify-between rounded-md border px-3 py-2 text-xs ${valid?'border-emerald-400/30 bg-emerald-400/5 text-emerald-300':type==='invoice'?'border-rose-400/30 bg-rose-400/5 text-rose-300':'border-amber-400/30 bg-amber-400/5 text-amber-300'}`}><span>{label}</span><span className="font-mono">{count}/{type==='invoice'?40:'60–80'} CHARS — {valid?'VALID':type==='invoice'?'OVER LIMIT':'CHECK RANGE'}</span></div> }
export default function Workbench(){ const { decrement } = usePendingQueue(); const [items,setItems]=useState(seed); const [selected,setSelected]=useState(seed[0]); const [query,setQuery]=useState(''); const [inspector,setInspector]=useState(false); const filtered=useMemo(()=>items.filter(i=>`${i.sku} ${i.name} ${i.category}`.toLowerCase().includes(query.toLowerCase())),[items,query]); const approve=()=>{setItems(all=>all.map(i=>i.sku===selected.sku?{...i,confidence:96,status:'Approved'}:i));setSelected({...selected,confidence:96,status:'Approved'});decrement();toast.success(`SKU #${selected.sku} approved and pushed to catalog`)}; return <AppShell title="Data workbench"><div className="mx-auto max-w-[1500px]"><SectionHeading eyebrow="Enrichment workspace" title="Review and refine records" description="Compare source values with AI-enriched catalog output before publishing." action={<button onClick={approve} className="inline-flex items-center gap-2 rounded-md bg-primary px-4 py-2.5 text-sm font-medium text-primary-foreground hover:opacity-90"><Check className="size-4" />Approve selected</button>} /><div className="mb-5 flex flex-wrap gap-3"><label className="flex min-w-64 flex-1 items-center gap-2 rounded-md border border-border bg-card px-3 py-2"><Search className="size-4 text-muted-foreground" /><input aria-label="Search records" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search SKU, description, category" className="w-full bg-transparent text-sm outline-none placeholder:text-muted-foreground" /></label><button className="flex items-center gap-2 rounded-md border border-border px-3 py-2 text-sm text-muted-foreground hover:bg-accent"><SlidersHorizontal className="size-4" />Filters <ChevronDown className="size-3" /></button></div><div className="grid gap-5 xl:grid-cols-[minmax(0,1fr)_420px]"><section className="panel overflow-hidden"><div className="flex items-center justify-between border-b border-border p-4"><div><p className="font-mono text-[10px] uppercase tracking-[0.18em] text-muted-foreground">Live records</p><p className="mt-1 text-sm font-medium">{filtered.length} of {items.length} records in view</p></div><span className="rounded-full bg-amber-300/10 px-2.5 py-1 font-mono text-[10px] text-amber-300">14 pending</span></div><div className="overflow-x-auto"><table className="w-full min-w-[720px] text-left text-sm"><thead className="bg-accent/30 font-mono text-[10px] uppercase tracking-wider text-muted-foreground"><tr><th className="px-4 py-3">SKU</th><th className="px-4 py-3">Product</th><th className="px-4 py-3">Category</th><th className="px-4 py-3">Confidence</th><th className="px-4 py-3">State</th><th /></tr></thead><tbody>{filtered.map(item=><tr key={item.sku} className={`cursor-pointer border-t border-border transition-colors hover:bg-accent/30 ${selected.sku===item.sku?'bg-cyan-300/5':''}`} onClick={()=>{setSelected(item);setInspector(true)}}><td className="px-4 py-4 font-mono text-xs text-cyan-300">#{item.sku}</td><td className="px-4 py-4 font-medium">{item.name}</td><td className="px-4 py-4 text-xs text-muted-foreground">{item.category}</td><td className="px-4 py-4"><span className={`font-mono text-xs ${item.confidence>=90?'text-emerald-300':'text-amber-300'}`}>{item.confidence}%</span></td><td className="px-4 py-4 text-xs">{item.status}</td><td className="px-4 py-4"><ArrowRight className="size-4 text-muted-foreground" /></td></tr>)}</tbody></table></div></section><Inspector item={selected} approve={approve} close={()=>setInspector(false)} compact={inspector} /></div></div></AppShell> }
function Inspector({item,approve,close,compact}:{item:Item;approve:()=>void;close:()=>void;compact:boolean}){return <aside className={`${compact?'compact-inspector fixed inset-x-3 bottom-3 z-30 max-h-[78vh] overflow-auto shadow-2xl':'wide-inspector'} panel p-5`}><div className="mb-5 flex items-start justify-between"><div><p className="font-mono text-[10px] uppercase tracking-[0.18em] text-cyan-300">Diff inspector</p><h3 className="mt-1 font-semibold">#{item.sku} · {item.name}</h3></div>{compact&&<button aria-label="Close inspector" onClick={close} className="rounded p-1 hover:bg-accent"><X className="size-4" /></button>}</div><div className="mb-5 flex items-center justify-between rounded-md bg-accent/40 p-3"><span className="text-xs text-muted-foreground">AI confidence</span><span className={`font-mono text-lg ${item.confidence>=90?'text-emerald-300':'text-amber-300'}`}>{item.confidence}%</span></div><div className="flex flex-col gap-4"><div><p className="mb-2 font-mono text-[10px] uppercase tracking-wider text-muted-foreground">Invoice description</p><div className="rounded-md border border-border bg-background/50 p-3 text-sm">{item.invoice}</div><div className="mt-2"><Counter label="Invoice desc" value={item.invoice} type="invoice" /></div></div><div><p className="mb-2 font-mono text-[10px] uppercase tracking-wider text-muted-foreground">Mobile description</p><div className="rounded-md border border-border bg-background/50 p-3 text-sm leading-6">{item.mobile}</div><div className="mt-2"><Counter label="Mobile desc" value={item.mobile} type="mobile" /></div></div></div><div className="mt-5 flex gap-2"><button onClick={approve} className="flex flex-1 items-center justify-center gap-2 rounded-md bg-emerald-400 px-3 py-2.5 text-sm font-medium text-emerald-950 hover:bg-emerald-300"><CheckCircle2 className="size-4" />Approve item</button><button className="rounded-md border border-border px-3 text-sm text-muted-foreground hover:bg-accent" aria-label="Open AI suggestions"><Sparkles className="size-4" /></button></div></aside>}
